apiVersion: apps/v1
kind: Deployment
metadata:
  name: wwhotel-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: wwhotel
  strategy:
    rollingUpdate: 
      maxSurge: 50% 
      maxUnavailable: 10%
    type: RollingUpdate 
  template:
    metadata:
      labels:
        app: wwhotel
    spec:
      containers:
      - name: wwhotel
        image: qiuer0121/wwhotel:latest
        ports:
        - containerPort: 9091

